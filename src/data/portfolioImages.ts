export interface PortfolioImage {
  src: string;
  alt: string;
}

/**
 * Add portfolio images here.
 * Import each image at the top and push it to the array below.
 *
 * Example:
 *   import myPhoto from "../assets/portfolio/photo1.jpg";
 *   { src: myPhoto, alt: "Bridal gown alteration" }
 */
const portfolioImages: PortfolioImage[] = [
  // Images will be added here
];

export default portfolioImages;
